cron:

- description: delete old stored facebook schedule image
  url: /onfacebook/clean/
  schedule: every 12 hours
  
- description: delete old stored schedule
  url: /cleanschedule
  schedule: every 12 hours